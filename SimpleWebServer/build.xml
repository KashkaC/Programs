<project name="SimpleWebServer" default="build" basedir=".">
	<description>
		Buildfile for the SimpleWebServer Kashka calvin. No default and runs build. 
	</description>
	<property name="src" location="src/"/>
	<property name="build" location="bin/"/>
	
	<target name="clean" description="deletes build directory">
		<delete dir="${build}"/>
		</target>
	
	<target name="build" description="compiles all java files to /bin">
		<mkdir dir="${build}"/>
		<javac srcdir="${src}" destdir="${build}"/>
	</target>
	<target name="run" description="launches SimpleWebServer on port 8080">
		<java classname="edu.nmsu.cs.webserver.WebServer" classpath="${build}" 
			fork="true" dir="${basedir}">
			<arg value="8080"/>
		</java>
	</target>
</project>